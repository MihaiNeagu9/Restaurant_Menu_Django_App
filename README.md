Restaurant Menu Django App

A full-stack web application built with **Django** that allows administrators to manage restaurant menu items (CRUD operations) and users to browse the digital menu.  
The app demonstrates database modeling, class-based views, and template rendering with a clean, responsive layout.
